import { TextFromObjectPipe } from './text-from-object.pipe';

describe('TextFromObjectPipe', () => {
  it('create an instance', () => {
    const pipe = new TextFromObjectPipe();
    expect(pipe).toBeTruthy();
  });
});
